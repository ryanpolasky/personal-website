{
  "name": "ryan-polasky-website",
  "version": "1.0.0",
  "description": "Ryan Polasky's personal website",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify --verbose",
    "dev": "tailwindcss -i ./src/input.css -o ./styles.css --watch",
    "build:debug": "tailwindcss -i ./src/input.css -o ./styles.css --verbose"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
